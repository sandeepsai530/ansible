- name: variables from file
  hosts: local
  connections: local
  vars_files:
  - course.yaml
  tasks:
  - name: print course information
    ansible.builtin.debug:
      msg: "course is {{ COURSE }}, duration is {{DURATION}} and trained by {{TRAINER}}"